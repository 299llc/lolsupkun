{
  "name": "rorusapo-cdk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:lambda": "esbuild lambda/gemini-proxy.ts --bundle --platform=node --target=node22 --outdir=lambda/dist --external:@aws-sdk/client-secrets-manager --minify",
    "cdk": "cdk",
    "predeploy": "npm run build:lambda",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "diff": "cdk diff"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.180.0",
    "esbuild": "^0.27.4",
    "typescript": "^5.8.0"
  }
}
